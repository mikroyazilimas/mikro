@model Telerik.Sitefinity.Frontend.Mvc.Models.ContentListViewModel


<div class="container-sub">
    <div class="container-sub-content">
        <!-- DÖNGÜ -->
        @foreach (var item in Model.Items.OrderByDescending(x => (DateTime?)x.Fields.DateTime))
        {
            <div class="container-sub-content-item">
                <div class="container-sub-content-item-content">
                    <div class="container-sub-content-item-content-left">
                        <div class="container-sub-content-item-content-left__date">
                            @if (item.Fields.DateTime != null && (DateTime?)item.Fields.DateTime != null)
                            {
                                <span>@(((DateTime?)item.Fields.DateTime).Value.ToString("dd MMMM yyyy", new System.Globalization.CultureInfo("tr-TR")))</span>
                            }

                        </div>
                        <div class="container-sub-content-item-content-left__title">
                            <span>@item.Fields.Title</span>
                        </div>
                        <div class="container-sub-content-item-content-left__text">
                            <span>@item.Fields.ShortText</span>
                        </div>
                    </div>
                    @if (item.Fields.DateTime != null && (DateTime?)item.Fields.DateCreated != null)
                    {
                        <a href="@String.Format("basinda-mikro/{0}{1}", (((DateTime?)item.Fields.DateCreated).Value.ToString("yyyy/MM/dd/")),item.Fields.UrlName)" target="_blank">
                            <div class="container-sub-content-item-content-right">
                                <i class="fas fa-eye container-sub-content-item-content-rigth__icon"></i>
                                <div class="container-sub-content-item-content-right__title">
                                    <span>Detayı Gör</span>
                                </div>
                            </div>
                        </a>
                    }

                    
                </div>
            </div>
        }

        <!-- //// -->
    </div>
</div>